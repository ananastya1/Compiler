routine max(a: real, b: real) : real is
    if a > b then
        return a;
    else
        return b;
    end;
end
var x: real is 10.50;
var y: real is 15.23;
var max_value: real is max(x, y);
write(max_value);
